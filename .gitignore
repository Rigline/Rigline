node_modules/
dist/
*.tsbuildinfo
*.vsix

# Written by `pnpm stage publish --report-summary`, read by the release workflow's summary step.
pnpm-publish-summary.json

# Scratch files the agent workflow writes; never content.
.commitmsg.tmp
.local/

# Worktrees Claude Code creates for this repo, which are checkouts of it.
.claude/worktrees/
