{
  "name": "@rigline/vscode",
  "version": "1.0.0-alpha.6",
  "description": "The companion VS Code extension: acquires the engine, watches for an extension update, re-injects",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../../scripts/clean-dist.mjs && rolldown -c rolldown.config.ts && node build-manifest.mjs",
    "package": "pnpm build && cd dist && vsce package --no-dependencies --allow-missing-repository --out ../rigline.vsix",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "rigline": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "1.90.0",
    "@vscode/vsce": "^4.0.0"
  }
}
