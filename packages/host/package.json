{
  "name": "@rigline/host",
  "version": "1.0.0-alpha.9",
  "description": "The injected loader: pre.js wraps the message bus before boot, post.js loads plugins after it",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rolldown -c rolldown.config.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@rigline/plugin-api": "workspace:*"
  }
}
