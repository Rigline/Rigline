{
  "name": "@rigline/host",
  "version": "1.0.0-alpha.11",
  "description": "The injected loader: pre.js wraps the message bus before boot, post.js loads plugins after it, and the runtime modules plugins import",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../../scripts/clean-dist.mjs && rolldown -c rolldown.config.ts",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.config.json"
  },
  "dependencies": {
    "@rigline/plugin-api": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  }
}
