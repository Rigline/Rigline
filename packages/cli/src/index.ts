#!/usr/bin/env node
/**
 * The prototype command. A thin surface over @prototype/core; phase 3 of docs/plan.md fills it in.
 */
import { CORE_VERSION } from "@prototype/core";

console.log(`prototype ${CORE_VERSION}`);
